{
  "name": "foodexpress",
  "version": "1.0.0",
  "description": "Simple food delivery app for Docker practice",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "supertest": "^7.0.0"
  }
}
